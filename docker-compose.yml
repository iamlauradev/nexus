services:

  nexus_backend:
    build: ./backend
    container_name: nexus_backend
    restart: unless-stopped
    environment:
      TZ: Europe/Madrid
      DB_HOST:     shared_postgres
      DB_PORT:     5432
      DB_NAME:     ${DB_NAME:-trackerdb}
      DB_USER:     ${DB_USER:-tracker}
      DB_PASSWORD: ${DB_PASSWORD}
      SECRET_KEY:  ${SECRET_KEY}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-10080}
      FRONTEND_URL: ${FRONTEND_URL:-https://nexus.iamlaura.dev}
      TMDB_API_KEY: ${TMDB_API_KEY:-}
      REDIS_HOST:     shared_redis
      REDIS_PORT:     6379
      REDIS_DB:       0
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      ANILIST_URL: ${ANILIST_URL:-https://graphql.anilist.co}
    networks:
      - homelab_net
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
        reservations:
          memory: 64M
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nexus-api.rule=Host(`nexus-api.iamlaura.dev`)"
      - "traefik.http.routers.nexus-api.entrypoints=websecure"
      - "traefik.http.routers.nexus-api.tls.certresolver=letsencrypt"
      - "traefik.http.routers.nexus-api.middlewares=security-headers@file,api-rate-limit@file"
      - "traefik.http.services.nexus-api.loadbalancer.server.port=8000"

  nexus_frontend:
    build:
      context: ./frontend
      args:
        API_URL: ${API_EXTERNAL_URL:-https://nexus-api.iamlaura.dev}
    container_name: nexus_frontend
    restart: unless-stopped
    environment:
      TZ: Europe/Madrid
    depends_on:
      - nexus_backend
    networks:
      - homelab_net
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: '0.1'
        reservations:
          memory: 8M
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:80"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nexus.rule=Host(`nexus.iamlaura.dev`)"
      - "traefik.http.routers.nexus.entrypoints=websecure"
      - "traefik.http.routers.nexus.tls.certresolver=letsencrypt"
      - "traefik.http.routers.nexus.middlewares=security-headers@file"
      - "traefik.http.services.nexus.loadbalancer.server.port=80"

networks:
  homelab_net:
    external: true
